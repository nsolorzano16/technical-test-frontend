export interface StudentsData {
  firstName: string;
  lastName: string;
  dob: Date;
  email: string;
  address: string;
  gender: string;
  id: string;
}

export interface RespDelete {
  ok: boolean;
  msg: string;
}
